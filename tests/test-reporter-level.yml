name: Test reporter and level inputs
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - id: action
        uses: .
        with:
          github_token: ${{ github.token }}
          reporter: local
          level: info
          filter_mode: nofilter
          fail_level: none
          yamllint_flags: tests/fixtures/sample.yaml

      - name: Assert action ran successfully with info level
        run: |
          echo "Action completed with reporter=local and level=info"
          echo "PASS"
